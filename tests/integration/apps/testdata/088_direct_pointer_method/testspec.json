{
  "description": "Like 087 but calls Write directly without fmt.Fprint adapter. Tests if cross-package pointer-receiver direct method dispatch preserves mutations.",
  "modulePath": "testpkg",
  "expected": "buffer=>>hello world<<"
}
