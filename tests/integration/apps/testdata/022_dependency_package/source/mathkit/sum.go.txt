package mathkit

func Sum(values ...int) int {
	total := 0
	for _, v := range values {
		total = accumulate(total, v)
	}
	return total
}

func accumulate(running, next int) int {
	return running + next
}
