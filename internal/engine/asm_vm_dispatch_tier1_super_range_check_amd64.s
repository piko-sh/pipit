// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerSubOpRangeCheckUintJumpFalse jumps if uints[C] < ext1.a or uints[C] > ext1.b; offset packed in ext2.
TEXT ·handlerSubOpRangeCheckUintJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    (SI)(BX*8), AX
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	MOVQ    DX, CX
	SHRQ    $8, CX
	MOVBLZX CL, CX
	SHRQ    $16, DX
	MOVBLZX DL, DI
	CMPQ    AX, CX
	JB      rangeCheckUintTakeJump
	CMPQ    AX, DI
	JA      rangeCheckUintTakeJump
	ADDQ    $6, R14
	JMP     rangeCheckUintDispatch
rangeCheckUintTakeJump:
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    $5, R14
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
rangeCheckUintDispatch:
	DISPATCH_NEXT()

// handlerSubOpEqUintConstJumpFalse jumps by the ext offset when uints[B] != C, stepping past the ext word and the trailing no-op either way.
TEXT ·handlerSubOpEqUintConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	SHRQ    $24, DX
	MOVBLZX DL, CX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    (SI)(BX*8), AX
	CMPQ    AX, CX
	JNE     eqUintConstTakeJump
	ADDQ    $2, R14
	JMP     eqUintConstDispatch
eqUintConstTakeJump:
	MOVL    (R12)(R14*4), DX
	ADDQ    $2, R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
eqUintConstDispatch:
	DISPATCH_NEXT()

// handlerLeIntConstJumpFalse compares ints[B] <= intConstants[C] and jumps if false.
TEXT ·handlerLeIntConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JLE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerLtIntConstJumpFalse compares ints[B] < intConstants[C] and jumps if false.
TEXT ·handlerLtIntConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JLT     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerEqIntConstJumpFalse compares ints[B] == intConstants[C] and jumps if false.
TEXT ·handlerEqIntConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JEQ     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerEqIntConstJumpTrue compares ints[B] == intConstants[C] and jumps if true.
TEXT ·handlerEqIntConstJumpTrue(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JNE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerGeIntConstJumpFalse compares ints[B] >= intConstants[C] and jumps if false.
TEXT ·handlerGeIntConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JGE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerGtIntConstJumpFalse compares ints[B] > intConstants[C] and jumps if false.
TEXT ·handlerGtIntConstJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R11)(BX*8)
	JGT     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerLtIntJumpFalse compares ints[B] < ints[C] and jumps if false.
TEXT ·handlerLtIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JLT     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerLeIntJumpFalse compares ints[B] <= ints[C] and jumps if false.
TEXT ·handlerLeIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JLE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerGtIntJumpFalse compares ints[B] > ints[C] and jumps if false.
TEXT ·handlerGtIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JGT     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerGeIntJumpFalse compares ints[B] >= ints[C] and jumps if false.
TEXT ·handlerGeIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JGE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerEqIntJumpFalse compares ints[B] == ints[C] and jumps if false.
TEXT ·handlerEqIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JEQ     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()

// handlerNeIntJumpFalse compares ints[B] != ints[C] and jumps if false.
TEXT ·handlerNeIntJumpFalse(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JNE     taken
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	JMP     dispatch

taken:
	INCQ    R14

dispatch:
	DISPATCH_NEXT()
