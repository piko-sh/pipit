// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerSubOpIntToFloat sets floats[B] = float64(ints[C]).
TEXT ·handlerSubOpIntToFloat(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	CVTSQ2SD (R8)(BX*8), X0
	MOVSD   X0, (R9)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpFloatToInt sets ints[B] = int64(floats[C]).
TEXT ·handlerSubOpFloatToInt(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	CVTTSD2SQ (R9)(BX*8), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpUintToFloat sets floats[B] = float64(uints[C]).
TEXT ·handlerSubOpUintToFloat(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_UINTS_BASE(R15), DI
	MOVQ    (DI)(BX*8), SI
	TESTQ   SI, SI
	JS      unsigned_to_float_high_bit_AX
	CVTSQ2SD SI, X0
	JMP     unsigned_to_float_done_AX
unsigned_to_float_high_bit_AX:
	MOVQ    SI, CX
	ANDQ    $1, CX
	SHRQ    $1, SI
	ORQ     CX, SI
	CVTSQ2SD SI, X0
	ADDSD   X0, X0
unsigned_to_float_done_AX:
	MOVSD   X0, (R9)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpFloatToUint sets uints[B] = uint64(floats[C]).
TEXT ·handlerSubOpFloatToUint(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVSD   (R9)(BX*8), X0
	MOVQ    $0x43E0000000000000, CX
	MOVQ    CX, X1
	UCOMISD X1, X0
	JB      float_to_unsigned_small_AX
	SUBSD   X1, X0
	CVTTSD2SQ X0, SI
	MOVQ    $0x8000000000000000, CX
	XORQ    CX, SI
	JMP     float_to_unsigned_done_AX
float_to_unsigned_small_AX:
	CVTTSD2SQ X0, SI
float_to_unsigned_done_AX:
	MOVQ    CTX_UINTS_BASE(R15), DI
	MOVQ    SI, (DI)(AX*8)
	DISPATCH_NEXT()
