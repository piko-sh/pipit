// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerSubIntConst sets ints[A] = ints[B] - intConstants[C].
TEXT ·handlerSubIntConst(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    (R8)(BX*8), SI
	SUBQ    (R11)(CX*8), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerAddIntConst sets ints[A] = ints[B] + intConstants[C].
TEXT ·handlerAddIntConst(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    (R8)(BX*8), SI
	ADDQ    (R11)(CX*8), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerMulIntConst sets ints[A] = ints[B] * intConstants[C].
TEXT ·handlerMulIntConst(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    (R8)(BX*8), SI
	IMULQ   (R11)(CX*8), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerAddIntJump sets ints[A] = ints[B] + intConstants[C] and unconditionally jumps.
TEXT ·handlerAddIntJump(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    (R8)(BX*8), SI
	ADDQ    (R11)(CX*8), SI
	MOVQ    SI, (R8)(AX*8)
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
	DISPATCH_NEXT()

// handlerIncIntJumpLt increments ints[B] and jumps if ints[B] < ints[C] in tier-1 form (subOpIncIntJumpLt).
TEXT ·handlerIncIntJumpLt(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	INCQ    (R8)(AX*8)
	MOVQ    (R8)(AX*8), SI
	CMPQ    SI, (R8)(BX*8)
	JLT     jump
	INCQ    R14
	JMP     dispatch

jump:
	MOVL    (R12)(R14*4), DX
	INCQ    R14
	SHRQ    $8, DX
	MOVWLZX DX, AX
	MOVWQSX AX, AX
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:

dispatch:
	DISPATCH_NEXT()

// handlerRangeNextSliceByte advances ints[A]; on end-of-range jumps by ext word; else uints[C] = byte at slicesByte[B][ints[A]].
TEXT ·handlerRangeNextSliceByte(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(AX*8), CX
	INCQ    CX
	MOVQ    CX, (R8)(AX*8)
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), DX
	CMPQ    CX, DX
	JGE     rangeByteEnd
	MOVQ    0(SI), SI
	MOVBLZX (SI)(CX*1), AX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    AX, (SI)(DI*8)
	INCQ    R14
	JMP     rangeByteDispatch
rangeByteEnd:
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	ANDQ    $0xFFFFFF, AX
	INCQ    R14
	ADDQ    AX, R14
	DECQ    CTX_POLL_BUDGET(R15)
	JG      poll_ok
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_POLL, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
poll_ok:
rangeByteDispatch:
	DISPATCH_NEXT()
