// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerGetStructFieldIntT0 sets ints[A] = receiver[B].field(layout[C]) for full-width scalar fields; other shapes fall back to handlerPathBShimGetStructFieldIntT0.
TEXT ·handlerGetStructFieldIntT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $2
	JE      sf_kind_ok
	CMPQ    CX, $6
	JNE     sf_shim
sf_kind_ok:
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    (DI), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CX, (R8)(AX*8)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldIntT0(SB)

// handlerSetStructFieldIntT0 sets receiver[A].field(layout[C]) = ints[B] for full-width scalar fields; other shapes fall back to handlerPathBShimSetStructFieldIntT0.
TEXT ·handlerSetStructFieldIntT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $2
	JE      sf_kind_ok
	CMPQ    CX, $6
	JNE     sf_shim
sf_kind_ok:
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    (R8)(AX*8), CX
	MOVQ    CX, (DI)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimSetStructFieldIntT0(SB)

// handlerGetStructFieldUintT0 sets uints[A] = receiver[B].field(layout[C]) for full-width scalar fields; other shapes fall back to handlerPathBShimGetStructFieldUintT0.
TEXT ·handlerGetStructFieldUintT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $7
	JE      sf_kind_ok
	CMPQ    CX, $11
	JNE     sf_shim
sf_kind_ok:
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    (DI), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    CX, (SI)(AX*8)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldUintT0(SB)

// handlerSetStructFieldUintT0 sets receiver[A].field(layout[C]) = uints[B] for full-width scalar fields; other shapes fall back to handlerPathBShimSetStructFieldUintT0.
TEXT ·handlerSetStructFieldUintT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $7
	JE      sf_kind_ok
	CMPQ    CX, $11
	JNE     sf_shim
sf_kind_ok:
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    (SI)(AX*8), CX
	MOVQ    CX, (DI)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimSetStructFieldUintT0(SB)

// handlerGetStructFieldFloatT0 sets floats[A] = receiver[B].field(layout[C]) for full-width scalar fields; other shapes fall back to handlerPathBShimGetStructFieldFloatT0.
TEXT ·handlerGetStructFieldFloatT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $14
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    (DI), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CX, (R9)(AX*8)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldFloatT0(SB)

// handlerSetStructFieldFloatT0 sets receiver[A].field(layout[C]) = floats[B] for full-width scalar fields; other shapes fall back to handlerPathBShimSetStructFieldFloatT0.
TEXT ·handlerSetStructFieldFloatT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $14
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    (R9)(AX*8), CX
	MOVQ    CX, (DI)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimSetStructFieldFloatT0(SB)

// handlerGetStructFieldBoolT0 sets bools[A] = receiver[B].field(layout[C]) for full-width scalar fields; other shapes fall back to handlerPathBShimGetStructFieldBoolT0.
TEXT ·handlerGetStructFieldBoolT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $1
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVBLZX (DI), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_BOOLS_BASE(R15), SI
	MOVB    CL, (SI)(AX*1)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldBoolT0(SB)

// handlerSetStructFieldBoolT0 sets receiver[A].field(layout[C]) = bools[B] for full-width scalar fields; other shapes fall back to handlerPathBShimSetStructFieldBoolT0.
TEXT ·handlerSetStructFieldBoolT0(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $1
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_BOOLS_BASE(R15), SI
	MOVBLZX (SI)(AX*1), CX
	MOVB    CL, (DI)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimSetStructFieldBoolT0(SB)

// handlerGetStructFieldSliceIndexScalar sets bank(ext2.C)[A] = receiver[B].field(layout[C])[ints[ext1.C]].subfield(ext2.wide); other shapes fall back to handlerPathBShimGetStructFieldSliceIndexScalar.
TEXT ·handlerGetStructFieldSliceIndexScalar(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $23
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVL    (R12)(R14*4), BX
	MOVL    4(R12)(R14*4), SI

	MOVQ    BX, AX
	SHRQ    $24, AX
	MOVQ    (R8)(AX*8), AX
	CMPQ    AX, 8(DI)
	JAE     sf_shim

	MOVQ    BX, CX
	SHRQ    $8, CX
	ANDQ    $0xFFFF, CX
	IMULQ   CX, AX
	ADDQ    (DI), AX
	MOVQ    SI, CX
	SHRQ    $8, CX
	ANDQ    $0xFFFF, CX
	ADDQ    CX, AX

	SHRQ    $24, SI
	MOVQ    DX, BX
	SHRQ    $8, BX
	MOVBLZX BL, BX

	CMPQ    SI, $REGKIND_INT
	JNE     sfsi_not_int
	MOVQ    (AX), CX
	MOVQ    CX, (R8)(BX*8)
	JMP     sfsi_done
sfsi_not_int:
	CMPQ    SI, $REGKIND_FLOAT
	JNE     sfsi_not_float
	MOVQ    (AX), CX
	MOVQ    CX, (R9)(BX*8)
	JMP     sfsi_done
sfsi_not_float:
	CMPQ    SI, $REGKIND_UINT
	JNE     sfsi_not_uint
	MOVQ    (AX), CX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    CX, (SI)(BX*8)
	JMP     sfsi_done
sfsi_not_uint:
	CMPQ    SI, $REGKIND_BOOL
	JNE     sf_shim
	MOVBLZX (AX), CX
	MOVQ    CTX_BOOLS_BASE(R15), SI
	MOVB    CL, (SI)(BX*1)
sfsi_done:
	ADDQ    $2, R14
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldSliceIndexScalar(SB)

// handlerGetStructFieldSliceLen sets ints[A] = len(receiver[B].field(layout[C])) for slice-kind fields; other shapes fall back to handlerPathBShimGetStructFieldSliceLen.
TEXT ·handlerGetStructFieldSliceLen(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_FRAME_POINTER(R15), AX
	IMULQ   $CALLFRAME_SIZE, AX
	ADDQ    CTX_CSTACK_BASE(R15), AX
	MOVQ    CF_FUNCTION(AX), AX
	CMPQ    BX, FN_STRUCT_LAYOUT_TABLE_LEN(AX)
	JAE     sf_shim
	MOVQ    FN_STRUCT_LAYOUT_TABLE(AX), SI
	SHLQ    $4, BX
	ADDQ    BX, SI
	CMPB    OFF_LAYOUT_FLAGS(SI), $0
	JNE     sf_shim
	MOVBLZX OFF_LAYOUT_KIND(SI), CX
	CMPQ    CX, $23
	JNE     sf_shim
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    CTX_GENERALS_BASE(R15), DI
	LEAQ    (AX)(AX*2), CX
	LEAQ    (DI)(CX*8), DI
	MOVQ    0(DI), CX
	TESTQ   CX, CX
	JZ      sf_shim
	MOVQ    16(DI), CX
	MOVQ    CX, BX
	ANDQ    $0x1F, BX
	CMPQ    BX, $22
	JE      sf_ptr_recv
	CMPQ    BX, $25
	JNE     sf_shim
	TESTQ   $0x100, CX
	JZ      sf_shim
	MOVQ    8(DI), DI
	JMP     sf_have_base
sf_ptr_recv:
	MOVQ    8(DI), DI
	TESTQ   $0x80, CX
	JZ      sf_have_base
	MOVQ    (DI), DI
sf_have_base:
	TESTQ   DI, DI
	JZ      sf_shim
	MOVLQZX OFF_LAYOUT_OFFSET(SI), CX
	ADDQ    CX, DI
	MOVQ    8(DI), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CX, (R8)(AX*8)
	DISPATCH_NEXT()
sf_shim:
	JMP     ·handlerPathBShimGetStructFieldSliceLen(SB)
