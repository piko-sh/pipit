// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerSubOpCap sets ints[B] = cap(value at C) via asmCallCap (collectionLengthOrCap).
TEXT ·handlerSubOpCap(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $32
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	CALL    ·asmCallCap(SB)
	MOVQ    24(SP), R15
	ADJSP   $-32
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpBytesToString sets strings[B] = string(bytes at C) via asmCallBytesToString (arena-backed).
TEXT ·handlerSubOpBytesToString(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $32
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	CALL    ·asmCallBytesToString(SB)
	MOVQ    24(SP), R15
	ADJSP   $-32
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpBoxSliceInt boxes []int64 at C into general[B] via asmCallBoxSliceInt (reflect.ValueOf).
TEXT ·handlerSubOpBoxSliceInt(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $32
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	CALL    ·asmCallBoxSliceInt(SB)
	MOVQ    24(SP), R15
	ADJSP   $-32
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceInt builds []int64 of length C and cap ext.A into general[B] via asmCallMakeSliceInt.
TEXT ·handlerSubOpMakeSliceInt(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceInt(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceFloat builds []float64 of length C and cap ext.A into general[B] via asmCallMakeSliceFloat.
TEXT ·handlerSubOpMakeSliceFloat(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceFloat(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceString builds []string of length C and cap ext.A into general[B] via asmCallMakeSliceString.
TEXT ·handlerSubOpMakeSliceString(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceString(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceBool builds []bool of length C and cap ext.A into general[B] via asmCallMakeSliceBool.
TEXT ·handlerSubOpMakeSliceBool(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceBool(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceUint builds []uint64 of length C and cap ext.A into general[B] via asmCallMakeSliceUint.
TEXT ·handlerSubOpMakeSliceUint(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceUint(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpMakeSliceByte builds []byte of length C and cap ext.A into general[B] via asmCallMakeSliceByte.
TEXT ·handlerSubOpMakeSliceByte(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallMakeSliceByte(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAllocStructLiteral carves a zeroed pointer-free struct literal from the arena generic-bytes slab into general[C]; falls back to Go via EXIT_TIER2.
TEXT ·handlerSubOpAllocStructLiteral(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVWLZX AX, AX

	CMPB    runtime·writeBarrier(SB), $0
	JNE     asl_fallback
	MOVQ    CTX_CURRENT_FRAME(R15), CX
	MOVQ    CF_FUNCTION(CX), CX
	MOVQ    FN_STRUCT_LITERAL_TABLE(CX), DX
	TESTQ   DX, DX
	JZ      asl_fallback
	CMPQ    AX, FN_STRUCT_LITERAL_TABLE_LEN(CX)
	JGE     asl_fallback
	MOVQ    $SLE_SIZE, CX
	IMULQ   AX, CX
	ADDQ    CX, DX
	MOVQ    SLE_TYPE_WORD(DX), SI
	TESTQ   SI, SI
	JZ      asl_fallback
	LEAQ    (BX)(BX*2), BX
	SHLQ    $3, BX
	ADDQ    CTX_GENERALS_BASE(R15), BX

	MOVQ    CTX_ARENA_GENERIC_IDX_PTR(R15), CX
	MOVQ    (CX), DI
	MOVQ    SLE_ALIGN_MASK(DX), AX
	ADDQ    AX, DI
	NOTQ    AX
	ANDQ    AX, DI
	MOVQ    DI, AX
	ADDQ    SLE_BYTES(DX), AX
	MOVQ    CTX_ARENA_GENERIC_HDR(R15), SI
	CMPQ    AX, 8(SI)
	JG      asl_fallback
	MOVQ    AX, (CX)
	MOVQ    SLE_BYTES(DX), CX
	ADDQ    CX, CTX_ARENA_GENERIC_CHARGED(R15)
	MOVB    $1, CTX_ARENA_BANKS_DIRTY(R15)
	ADDQ    (SI), DI
	MOVQ    DI, AX

asl_zero_words:
	CMPQ    CX, $8
	JL      asl_zero_bytes
	MOVQ    $0, (AX)
	ADDQ    $8, AX
	SUBQ    $8, CX
	JMP     asl_zero_words
asl_zero_bytes:
	TESTQ   CX, CX
	JZ      asl_store
	MOVB    $0, (AX)
	INCQ    AX
	DECQ    CX
	JMP     asl_zero_bytes

asl_store:
	MOVQ    SLE_TYPE_WORD(DX), SI
	MOVQ    SI, (BX)
	MOVQ    DI, 8(BX)
	MOVQ    SLE_FLAG(DX), AX
	MOVQ    AX, 16(BX)
	INCQ    R14
	DISPATCH_NEXT()

asl_fallback:
	DECQ    R14
	MOVQ    R14, CTX_PC(R15)
	MOVQ    $EXIT_TIER2, CTX_EXIT_REASON(R15)
	MOVQ    R14, CTX_EXIT_PC(R15)
	RET     
