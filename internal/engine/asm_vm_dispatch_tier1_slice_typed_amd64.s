// Code generated by cmd/asmgen; DO NOT EDIT.

// Copyright 2026 PolitePixels Limited
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// This project stands against fascism, authoritarianism, and all forms of
// oppression. We built this to empower people, not to enable those who would
// strip others of their rights and dignity.

//go:build !safe && !(js && wasm) && amd64

#include "textflag.h"
#include "funcdata.h"
#include "asm_dispatch_offsets.h"
#include "asm_dispatch_amd64.h"

// handlerSliceGetIntDirect sets ints[A] = slicesInt[B][ints[C]] with bounds check.
TEXT ·handlerSliceGetIntDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVQ    (DI)(BX*8), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CX, (R8)(AX*8)
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSliceSetIntDirect sets slicesInt[A][ints[B]] = ints[C] with bounds check.
TEXT ·handlerSliceSetIntDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVQ    DX, AX
	SHRQ    $24, AX
	MOVQ    (R8)(AX*8), CX
	MOVQ    CX, (DI)(BX*8)
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSliceGetIntDirectUnchecked sets ints[A] = slicesInt[B][ints[C]] without a bounds check (BCE-proven).
TEXT ·handlerSliceGetIntDirectUnchecked(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    (DI)(BX*8), CX
	MOVQ    DX, AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CX, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSliceSetIntDirectUnchecked sets slicesInt[A][ints[B]] = ints[C] without a bounds check (BCE-proven).
TEXT ·handlerSliceSetIntDirectUnchecked(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    DX, AX
	SHRQ    $24, AX
	MOVQ    (R8)(AX*8), CX
	MOVQ    CX, (DI)(BX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceIntDirect sets ints[B] = int64(len(slicesInt[C])).
TEXT ·handlerSubOpLenSliceIntDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceFloatDirect sets ints[B] = int64(len(slicesFloat[C])).
TEXT ·handlerSubOpLenSliceFloatDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceStringDirect sets ints[B] = int64(len(slicesString[C])).
TEXT ·handlerSubOpLenSliceStringDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceBoolDirect sets ints[B] = int64(len(slicesBool[C])).
TEXT ·handlerSubOpLenSliceBoolDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceUintDirect sets ints[B] = int64(len(slicesUint[C])).
TEXT ·handlerSubOpLenSliceUintDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpLenSliceByteDirect sets ints[B] = int64(len(slicesByte[C])).
TEXT ·handlerSubOpLenSliceByteDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    8(SI), SI
	MOVQ    SI, (R8)(AX*8)
	DISPATCH_NEXT()

// handlerSubOpSliceGetFloatDirect sets floats[B] = slicesFloat[C][ints[ext.A]] with bounds check.
TEXT ·handlerSubOpSliceGetFloatDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVL    (R12)(R14*4), BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVSD   (DI)(BX*8), X0
	MOVSD   X0, (R9)(AX*8)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSetFloatDirect sets slicesFloat[B][ints[C]] = floats[ext.A] with bounds check.
TEXT ·handlerSubOpSliceSetFloatDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVSD   (R9)(AX*8), X0
	MOVSD   X0, (DI)(BX*8)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceGetUintDirect sets uints[B] = slicesUint[C][ints[ext.A]] with bounds check.
TEXT ·handlerSubOpSliceGetUintDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVL    (R12)(R14*4), BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVQ    (DI)(BX*8), CX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    CX, (SI)(AX*8)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSetUintDirect sets slicesUint[B][ints[C]] = uints[ext.A] with bounds check.
TEXT ·handlerSubOpSliceSetUintDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    (SI)(AX*8), AX
	MOVQ    AX, (DI)(BX*8)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceGetBoolDirect sets bools[B] = slicesBool[C][ints[ext.A]] with bounds check.
TEXT ·handlerSubOpSliceGetBoolDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVL    (R12)(R14*4), BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVB    (DI)(BX*1), CL
	MOVQ    CTX_BOOLS_BASE(R15), SI
	MOVB    CL, (SI)(AX*1)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSetBoolDirect sets slicesBool[B][ints[C]] = bools[ext.A] with bounds check.
TEXT ·handlerSubOpSliceSetBoolDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_BOOLS_BASE(R15), SI
	MOVB    (SI)(AX*1), AL
	MOVB    AL, (DI)(BX*1)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceGetStringDirect sets strings[B] = slicesString[C][ints[ext.A]] with bounds check.
TEXT ·handlerSubOpSliceGetStringDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVL    (R12)(R14*4), BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	SHLQ    $4, BX
	MOVQ    0(DI)(BX*1), CX
	MOVQ    8(DI)(BX*1), DX
	MOVQ    CTX_STRINGS_BASE(R15), SI
	SHLQ    $4, AX
	MOVQ    CX, 0(SI)(AX*1)
	MOVQ    DX, 8(SI)(AX*1)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceGetByteDirect sets uints[B] = uint64(slicesByte[C][ints[ext.A]]) with bounds check.
TEXT ·handlerSubOpSliceGetByteDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVL    (R12)(R14*4), BX
	SHRQ    $8, BX
	MOVBLZX BL, BX
	MOVQ    (R8)(BX*8), BX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVBLZX (DI)(BX*1), CX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    CX, (SI)(AX*8)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSetByteDirect sets slicesByte[B][ints[C]] = byte(uints[ext.A]) with bounds check.
TEXT ·handlerSubOpSliceSetByteDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVQ    (R8)(BX*8), BX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	CMPQ    BX, CX
	JAE     bounds_fail
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	MOVQ    CTX_UINTS_BASE(R15), SI
	MOVQ    (SI)(AX*8), AX
	MOVB    AL, (DI)(BX*1)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceByteSlice sets slicesByte[A] = slicesByte[C][ints[ext.b]:ints[ext.c]] (no max-bound) inline.
TEXT ·handlerSubOpSliceByteSlice(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	ADDQ    CX, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpMoveSliceInt copies slicesInt[C] to slicesInt[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceInt(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpMoveSliceFloat copies slicesFloat[C] to slicesFloat[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceFloat(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpMoveSliceString copies slicesString[C] to slicesString[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceString(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpMoveSliceBool copies slicesBool[C] to slicesBool[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceBool(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpMoveSliceUint copies slicesUint[C] to slicesUint[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceUint(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpMoveSliceByte copies slicesByte[C] to slicesByte[B] for the tier-1 typed-slice header move sub-op.
TEXT ·handlerSubOpMoveSliceByte(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    0(SI), DI
	MOVQ    8(SI), CX
	MOVQ    16(SI), DX
	MOVQ    CTX_SLICES_BYTE_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    DI, 0(SI)
	MOVQ    CX, 8(SI)
	MOVQ    DX, 16(SI)
	DISPATCH_NEXT()

// handlerSubOpSliceSliceIntDirect performs slicesInt[A] = slicesInt[C][low:high] with bounds check (low+high only).
TEXT ·handlerSubOpSliceSliceIntDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	MOVQ    CX, R10
	SHLQ    $3, R10
	ADDQ    R10, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_INT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSliceFloatDirect performs slicesFloat[A] = slicesFloat[C][low:high] with bounds check (low+high only).
TEXT ·handlerSubOpSliceSliceFloatDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	MOVQ    CX, R10
	SHLQ    $3, R10
	ADDQ    R10, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_FLOAT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSliceStringDirect performs slicesString[A] = slicesString[C][low:high] with bounds check (low+high only).
TEXT ·handlerSubOpSliceSliceStringDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	MOVQ    CX, R10
	SHLQ    $4, R10
	ADDQ    R10, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_STRING_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSliceBoolDirect performs slicesBool[A] = slicesBool[C][low:high] with bounds check (low+high only).
TEXT ·handlerSubOpSliceSliceBoolDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	ADDQ    CX, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_BOOL_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpSliceSliceUintDirect performs slicesUint[A] = slicesUint[C][low:high] with bounds check (low+high only).
TEXT ·handlerSubOpSliceSliceUintDirect(SB), NOSPLIT|NOFRAME, $0
	MOVQ    DX, AX
	SHRQ    $16, AX
	MOVBLZX AL, AX
	MOVQ    DX, BX
	SHRQ    $24, BX
	MOVBLZX BL, BX
	MOVL    (R12)(R14*4), DX
	MOVQ    DX, CX
	MOVBLZX CL, CX
	CMPQ    CX, $3
	JNE     bounds_fail
	MOVQ    DX, CX
	SHRQ    $16, CX
	MOVBLZX CL, CX
	MOVQ    DX, DI
	SHRQ    $24, DI
	MOVBLZX DIB, DI
	MOVQ    (R8)(CX*8), CX
	MOVQ    (R8)(DI*8), DI
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, BX
	ADDQ    BX, SI
	MOVQ    16(SI), DX
	TESTQ   CX, CX
	JS      bounds_fail
	CMPQ    CX, DI
	JG      bounds_fail
	CMPQ    DI, DX
	JG      bounds_fail
	MOVQ    0(SI), BX
	MOVQ    CX, R10
	SHLQ    $3, R10
	ADDQ    R10, BX
	SUBQ    CX, DI
	SUBQ    CX, DX
	MOVQ    CTX_SLICES_UINT_BASE(R15), SI
	IMULQ   $24, AX
	ADDQ    AX, SI
	MOVQ    BX, 0(SI)
	MOVQ    DI, 8(SI)
	MOVQ    DX, 16(SI)
	INCQ    R14
	DISPATCH_NEXT()
bounds_fail:
	JMP     ·pathBFallback(SB)

// handlerSubOpAppendSliceIntDirect - 3-operand shim wrapping ·asmCallAppendSliceIntDirect (typed-bank []int64 append).
TEXT ·handlerSubOpAppendSliceIntDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceIntDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAppendSliceFloatDirect - 3-operand shim wrapping ·asmCallAppendSliceFloatDirect (typed-bank []float64 append).
TEXT ·handlerSubOpAppendSliceFloatDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceFloatDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAppendSliceStringDirect - 3-operand shim wrapping ·asmCallAppendSliceStringDirect (typed-bank []string append).
TEXT ·handlerSubOpAppendSliceStringDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceStringDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAppendSliceBoolDirect - 3-operand shim wrapping ·asmCallAppendSliceBoolDirect (typed-bank []bool append).
TEXT ·handlerSubOpAppendSliceBoolDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceBoolDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAppendSliceUintDirect - 3-operand shim wrapping ·asmCallAppendSliceUintDirect (typed-bank []uint64 append).
TEXT ·handlerSubOpAppendSliceUintDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceUintDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()

// handlerSubOpAppendSliceByteDirect - 3-operand shim wrapping ·asmCallAppendSliceByteDirect (typed-bank []byte append).
TEXT ·handlerSubOpAppendSliceByteDirect(SB), NOSPLIT|NOFRAME, $0
	NO_LOCAL_POINTERS
	MOVQ    DX, BX
	SHRQ    $16, BX
	MOVBLZX BL, BX
	MOVQ    DX, CX
	SHRQ    $24, CX
	MOVL    (R12)(R14*4), AX
	SHRQ    $8, AX
	MOVBLZX AL, AX
	INCQ    R14
	MOVQ    R14, CTX_SAVED_PC(R15)
	ADJSP   $40
	MOVQ    R15, 0(SP)
	MOVQ    BX, 8(SP)
	MOVQ    CX, 16(SP)
	MOVQ    AX, 24(SP)
	CALL    ·asmCallAppendSliceByteDirect(SB)
	MOVQ    32(SP), R15
	ADJSP   $-40
	MOVQ    CTX_SAVED_PC(R15), R14
	MOVQ    CTX_CODE_BASE(R15), R12
	MOVQ    CTX_CODE_LEN(R15), R13
	MOVQ    CTX_INTS_BASE(R15), R8
	MOVQ    CTX_FLOATS_BASE(R15), R9
	MOVQ    CTX_INT_CONSTS_BASE(R15), R11
	MOVQ    CTX_JUMP_TABLE(R15), R10
	DISPATCH_NEXT()
